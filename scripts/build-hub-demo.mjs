import {
  copyFileSync,
  existsSync,
  mkdirSync,
  readFileSync,
  readdirSync,
  rmSync,
  statSync,
  writeFileSync,
} from "node:fs";
import { mkdtempSync } from "node:fs";
import { tmpdir } from "node:os";
import { dirname, join, relative } from "node:path";
import { fileURLToPath } from "node:url";
import { execFileSync } from "node:child_process";

const root = join(dirname(fileURLToPath(import.meta.url)), "..");
const outputDir = join(root, "public", "hub");
const hubLogoSource = join(root, "public", "mainline_logo.svg");
const hubLogoAsset = join(outputDir, "assets", "mainline_logo.svg");
const hubSourceHref = "https://github.com/mainline-org/mainline";
const hubBrandingCss = `

/* ----- Website Hub branding ----- */
.hub-branding {
  display: grid;
  gap: 4px;
  min-width: 0;
}

.hub-logo-lockup {
  display: inline-flex;
  width: fit-content;
  align-items: center;
  gap: 10px;
  color: var(--fg);
  text-decoration: none;
}

.hub-logo-lockup:hover {
  color: var(--fg);
  text-decoration: none;
}

.hub-logo {
  display: block;
  width: 40px;
  height: 40px;
  flex: 0 0 40px;
  border: 1px solid var(--line-strong);
  border-radius: 8px;
  background: var(--bg);
  box-shadow: 2px 2px 0 rgba(24, 25, 27, 0.12);
  object-fit: cover;
}

.hub-logo-lockup span {
  min-width: 0;
}

.hub-logo-lockup .brand,
.hub-logo-lockup .meta {
  display: block;
}

.hub-origin {
  max-width: 420px;
  color: var(--muted);
  font-size: 0.82em;
}

.hub-origin a {
  font-weight: 600;
}

@media (max-width: 720px) {
  .hub-branding {
    width: 100%;
  }

  .hub-origin {
    max-width: none;
  }
}
`;

function quote(value) {
  return `'${String(value).replaceAll("'", "'\\''")}'`;
}

function run(command, options = {}) {
  execFileSync("bash", ["-lc", command], {
    cwd: options.cwd ?? root,
    env: process.env,
    stdio: "inherit",
  });
}

function capture(command, options = {}) {
  return execFileSync("bash", ["-lc", command], {
    cwd: options.cwd ?? root,
    env: process.env,
    encoding: "utf8",
    stdio: ["ignore", "pipe", "inherit"],
  }).trim();
}

function fail(message) {
  throw new Error(`[mainline-hub] ${message}`);
}

function runStep(command, options = {}) {
  try {
    run(command, options);
    return true;
  } catch (error) {
    const label = options.label ?? "command";
    if (hubRequired) {
      throw new Error(`[mainline-hub] ${label} failed. ${error.message}`);
    }
    console.warn(`[mainline-hub] ${label} failed; continuing without live Hub data.`);
    return false;
  }
}

function parseSemver(version) {
  const match = String(version).match(/^v?(\d+)\.(\d+)\.(\d+)/);
  if (!match) {
    return null;
  }
  return match.slice(1).map((part) => Number(part));
}

function compareSemver(left, right) {
  const leftParts = parseSemver(left);
  const rightParts = parseSemver(right);
  if (!leftParts || !rightParts) {
    return 0;
  }
  for (let index = 0; index < 3; index += 1) {
    if (leftParts[index] !== rightParts[index]) {
      return leftParts[index] - rightParts[index];
    }
  }
  return 0;
}

function validateExportedHub() {
  const intentsPath = join(outputDir, "data", "intents.json");
  if (!existsSync(intentsPath)) {
    if (hubRequired) {
      fail(`missing Hub data file: ${intentsPath}`);
    }
    return;
  }

  const parsed = JSON.parse(readFileSync(intentsPath, "utf8"));
  const intents = Array.isArray(parsed) ? parsed : parsed.intents ?? [];
  const statusCounts = intents.reduce((counts, intent) => {
    const status = intent.status ?? "unknown";
    counts[status] = (counts[status] ?? 0) + 1;
    return counts;
  }, {});

  console.log(`[mainline-hub] exported intent statuses: ${JSON.stringify(statusCounts)}`);
  if (hubRequired && intents.length > 0 && !statusCounts.merged) {
    fail("Hub export produced zero merged intents; check that the Mainline source clone has full commit history");
  }
}

function listFiles(dir) {
  const entries = readdirSync(dir, { withFileTypes: true });
  const files = [];
  for (const entry of entries) {
    const fullPath = join(dir, entry.name);
    if (entry.isDirectory()) {
      files.push(...listFiles(fullPath));
      continue;
    }
    if (entry.isFile()) {
      files.push(fullPath);
    }
  }
  return files;
}

function pathToAsset(fromFile, assetPath) {
  return relative(dirname(fromFile), assetPath).replaceAll("\\", "/");
}

function hubOriginCopy(lang) {
  if (lang === "zh") {
    return `由 Mainline 从 GitHub 仓库 <a href="${hubSourceHref}">mainline-org/mainline</a> 生成。`;
  }
  return `Generated by Mainline from the GitHub repo <a href="${hubSourceHref}">mainline-org/mainline</a>.`;
}

function enhanceExportedHub() {
  const assetsDir = join(outputDir, "assets");
  const stylePath = join(assetsDir, "style.css");
  if (!existsSync(outputDir) || !statSync(outputDir).isDirectory()) {
    return;
  }
  if (!existsSync(hubLogoSource)) {
    if (hubRequired) {
      fail(`missing Hub logo source: ${hubLogoSource}`);
    }
    console.warn(`[mainline-hub] missing logo source: ${hubLogoSource}`);
    return;
  }
  if (!existsSync(stylePath)) {
    if (hubRequired) {
      fail(`missing Hub stylesheet: ${stylePath}`);
    }
    console.warn(`[mainline-hub] missing Hub stylesheet: ${stylePath}`);
    return;
  }

  mkdirSync(assetsDir, { recursive: true });
  copyFileSync(hubLogoSource, hubLogoAsset);

  const style = readFileSync(stylePath, "utf8");
  if (!style.includes("Website Hub branding")) {
    writeFileSync(stylePath, `${style}${hubBrandingCss}`);
  }

  let patched = 0;
  const headerPattern = /<header class="topbar">\n  <div>\n    <div class="brand">Mainline Hub<\/div>\n    <div class="meta">([^<]+)<\/div>\n  <\/div>/;
  for (const htmlPath of listFiles(outputDir).filter((file) => file.endsWith(".html"))) {
    const html = readFileSync(htmlPath, "utf8");
    const lang = html.includes(`<html lang="zh">`) ? "zh" : "en";
    const logoHref = pathToAsset(htmlPath, hubLogoAsset);
    const next = html.replace(headerPattern, (_, tagline) => `<header class="topbar">
  <div class="hub-branding">
    <a class="hub-logo-lockup" href="${hubSourceHref}" aria-label="mainline-org/mainline on GitHub">
      <img class="hub-logo" src="${logoHref}" alt="" width="40" height="40" decoding="async">
      <span>
        <span class="brand">Mainline Hub</span>
        <span class="meta">${tagline}</span>
      </span>
    </a>
    <div class="hub-origin">${hubOriginCopy(lang)}</div>
  </div>`);
    if (next !== html) {
      writeFileSync(htmlPath, next);
      patched += 1;
    }
  }

  if (patched === 0) {
    if (hubRequired) {
      fail("Hub branding injection did not match any HTML files");
    }
    console.warn("[mainline-hub] Hub branding injection did not match any HTML files.");
    return;
  }
  console.log(`[mainline-hub] branded ${patched} Hub HTML files`);
}

if (process.env.MAINLINE_BUILD_HUB !== "1") {
  process.exit(0);
}

const hubRequired = process.env.MAINLINE_HUB_REQUIRED === "1";
const minVersion = process.env.MAINLINE_MIN_VERSION;
const workDir = mkdtempSync(join(tmpdir(), "mainline-website-hub-"));
const providedSource = process.env.MAINLINE_HUB_SOURCE_DIR;
const sourceDir = providedSource || join(workDir, "mainline");
let mainlineCommand = process.env.MAINLINE_COMMAND;
let commandCwd = process.env.MAINLINE_COMMAND_CWD || root;

try {
  rmSync(outputDir, { recursive: true, force: true });

  if (!providedSource) {
    const cloned = runStep(
      `git clone --config gc.auto=0 https://github.com/mainline-org/mainline.git ${quote(sourceDir)}`,
      { label: "clone mainline source" },
    );
    if (!cloned) {
      process.exit(0);
    }
  }

  if (!mainlineCommand) {
    const installDir = join(workDir, "bin");
    mkdirSync(installDir, { recursive: true });
    const installed = runStep(
      `curl -fsSL https://raw.githubusercontent.com/mainline-org/mainline/main/install.sh | MAINLINE_INSTALL_DIR=${quote(installDir)} bash`,
      { label: "install mainline binary" },
    );
    if (!installed) {
      process.exit(0);
    }
    mainlineCommand = quote(join(installDir, "mainline"));
  }

  if (!existsSync(sourceDir)) {
    if (hubRequired) {
      fail(`source repo not found: ${sourceDir}`);
    }
    console.warn(`[mainline-hub] source repo not found: ${sourceDir}`);
    process.exit(0);
  }

  const versionText = capture(`${mainlineCommand} version --json`, { cwd: commandCwd });
  console.log(`[mainline-hub] mainline version: ${versionText}`);
  const versionData = JSON.parse(versionText);
  const version = versionData?.data?.version;
  if (!version) {
    fail("unable to read mainline version");
  }
  if (minVersion && compareSemver(version, minVersion) < 0) {
    fail(`mainline ${version} is older than required ${minVersion}`);
  }

  const syncOK = runStep(
    `${mainlineCommand} --cwd ${quote(sourceDir)} sync --json`,
    { cwd: commandCwd, label: "mainline sync" },
  );
  if (!syncOK) {
    process.exit(0);
  }

  const exportOK = runStep(
    `${mainlineCommand} --cwd ${quote(sourceDir)} --no-sync hub export ${quote(outputDir)} --json`,
    { cwd: commandCwd, label: "mainline hub export" },
  );
  if (!exportOK) {
    rmSync(outputDir, { recursive: true, force: true });
  } else {
    enhanceExportedHub();
  }
  validateExportedHub();
} finally {
  if (!providedSource) {
    rmSync(workDir, { recursive: true, force: true });
  }
}
